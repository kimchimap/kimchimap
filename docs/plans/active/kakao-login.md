# 카카오 로그인과 브라우저 인증

- 목표: 공식 OIDC 코드 교환과 서비스 세션을 연결하고 브라우저에서 안전하게 복구·폐기한다.
- 현재 브랜치: feat/kakao-login
- 기준 dev: bee2709197a4aa930a909c72b9d833feab87084e
- 완료: P06a PR #6 병합, 임시 브랜치 삭제, 원래 dev 동기화.
- 범위: PKCE S256·일회성 state·nonce, Redis에 브라우저별 인가 요청 임시 저장, 공급자 토큰 비보관, 로그인 UI와 메모리 토큰·single-flight·탭 잠금·사용자 캐시 정리.
- 수용 기준: 통제된 OIDC 공급자로 실제 코드 교환과 잘못된 state/nonce/서명 거부, Redis 장애, 브라우저 인증 실패·경합 테스트, 전체 verify, 문서 갱신, dev PR 병합·정리.
- 결정: 공급자 주소는 공식 고정값이며 테스트 대체는 test 소스 설정에서만 한다. 공개 키 조회와 코드 교환에 시간 제한을 둔다. 이메일·프로필을 요구하지 않는다.
- 실행 기록: Git clean 및 기준 커밋 확인. Spring Security와 카카오 공식 문서 재확인. 새 테스트는 아직 미실행.
- 외부 조건: 사용자가 키·도메인·콜백·OIDC 설정을 완료했다고 확인했다. 실제 사용자 로그인 성공은 아직 확인하지 않았다.
- 다음 작업: 서버 OAuth 설정과 인가 요청 저장소, 통제된 공급자 테스트, 브라우저 연결.

## 구현·검증 결과

- 서버 OAuth 설정·일회성 Redis 요청·PKCE/OIDC·공급자 비보관·요청 제한과 장애 거부 구현.
- 로그인·복구·로그아웃 화면과 브라우저 인증 모듈 구현. 사용자별 캐시는 private 접두사로 분리한다.
- 전체 verify exit 0, 서버 통합 45·단위 26·계약 5·성능 1·프론트 16·브라우저 6·하네스 28 통과. 상세는 validation.md.
- 완료 절차: 최종 계약·PR 준비 검증 exit 0, PR #7 squash 병합(843a3a5), 임시 브랜치 로컬·원격 삭제, 원래 dev 동기화. 다음 작업은 P07a 즐겨찾기와 증빙 이미지.
