# 업소와 원산지 모델 구현

- 목표: 업소·품목·용도·확장 카탈로그·정규화 원산지 기록과 공개 조회를 구현한다.
- 범위: 추가 Flyway 마이그레이션, 읽기 API, 원산지 표현 검증, 공개 판정, 테스트와 생성 API 계약.
- 제외: 공간 조합 검색(P04), 수집(P05), 로그인(P06), 관리자 쓰기 API(P07). 내부 검증된 서비스 경계는 이 작업에서 준비한다.
- 수용 기준: 실제 PostGIS 빈 DB 초기화, 국가 관계 무결성, 같은 범위의 상충·만료·미확인 처리, 민감 근거 비노출, DTO 응답, 잘못된 ID 400/없는 업소 404, 카탈로그 조회, 기존 검증 회귀.
- 현재 브랜치: feat/origin-model
- 기준 dev: 089d8e1b10c9f016726689873d2948ca87758eaa
- 완료: PR #2 병합, 이전 작업 브랜치 로컬·원격 삭제.
- 완료한 구현: V2 모델, 확장 카탈로그 JPA 조회, 업소 상세 SQL projection, 범위별 공개 판정, 비공개 근거 분리, 400/404/422 오류.
- 남은 작업: PR 준비·병합·브랜치 정리.
- 결정: 내부 식별자는 UUID, 개별 국가/성분은 정규화, 알 수 없는 비율은 NULL. 다운로드 시각을 관찰 시각으로 사용하지 않는다.
- 실행 기록: branch-start, branch-cleanup 성공. 나머지 검증은 실행 후 기록한다.
- 외부 조건: 키 4개는 공통 로컬 비밀 파일에 저장됨. 값은 문서에 남기지 않음. 카카오 앱 설정은 사용자 완료 확인. 공공 API 활용 승인도 사용자 완료 확인. 실제 외부 호출은 아직 미확인. 협회 수집 허가는 미확인.
- 실행 결과: 백엔드 단위/아키텍처 12개, 실제 PostGIS·Redis 통합 13개 통과(실패/skip 0). 초기 JDBC Instant 바인딩 오류는 OffsetDateTime UTC로 수정 후 재검증했다.
- 전체 작업 단위: verify-unit origin-model 통과. 하네스 28개, 백엔드 25개, 프론트 2개, E2E 2개 및 API 생성 비교·빌드 성공.
- 자기 검토: Controller의 영속 계층 접근 없음, SQL 바인딩, 동일 DB snapshot에서 상세 일괄 조회, 비공개 주장에 의한 상충 누락 차단, 잘린 후보의 긍정 판정 금지 확인.
- 다음 작업: PR 병합 후 P04 공간 검색.
